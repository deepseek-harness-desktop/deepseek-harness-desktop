# Desktop packages

Desktop-only runtime integrations for DeepSeek Harness.

The `ipc-server` package exposes the JSON-RPC server used by the Tauri desktop sidecar. It translates protocol requests into the existing API proxy and keeps application logic in the TypeScript runtime.
